# RESTful API 开发规范（中文团队强制执行）

> 本规范由技术架构组制定，所有后端开发人员必须严格遵守，无例外。
> 违反本规范的 PR 一律打回，不接受"临时方案"或"后续优化"的借口。

---

## 一、统一响应体格式

### 1.1 所有接口必须返回以下结构，禁止任何变体

```json
{
  "code": 0,
  "message": "success",
  "data": null
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| `code` | `number` | 业务状态码。0 = 成功，非 0 = 失败。**严禁用 HTTP 状态码充当此字段** |
| `message` | `string` | 面向调用方的可读提示。成功时固定 `"success"`，失败时为具体错误描述 |
| `data` | `any` | 业务数据。无数据时返回 `null`，**严禁省略此字段**，严禁返回空对象 `{}` 代替 `null` |

### 1.2 绝对禁止的写法

```javascript
// ❌ 直接返回裸数据
return { name: "张三", age: 28 };

// ❌ 字段名不一致
return { status: 0, msg: "ok", result: {} };

// ❌ 成功时省略 data
return { code: 0, message: "success" };

// ❌ 用空对象代替 null
return { code: 0, message: "success", data: {} };
```

### 1.3 正确示例

```javascript
// ✅ 单条查询
{ "code": 0, "message": "success", "data": { "id": 1, "name": "张三" } }

// ✅ 列表查询
{ "code": 0, "message": "success", "data": { "list": [...], "total": 100, "page": 1, "pageSize": 20 } }

// ✅ 无数据
{ "code": 0, "message": "success", "data": null }

// ✅ 业务失败
{ "code": 10001, "message": "用户余额不足", "data": null }
```

---

## 二、错误码规范

### 2.1 核心原则：业务错误码与 HTTP 状态码严格分离

- **HTTP 状态码**：只反映传输层面是否成功。业务接口一律返回 `200`，由 `code` 字段表达业务结果。
- **业务错误码**：由 `code` 字段承载，与 HTTP 状态码完全独立。

> 禁止用 HTTP 404 表达"用户不存在"，禁止用 HTTP 403 表达"权限不足"。
> 这些都是业务逻辑，用 code 表达。HTTP 层面只负责"请求是否到达了服务器"。

### 2.2 错误码分段规则

| 错误码范围 | 模块 | 示例 |
|-----------|------|------|
| `0` | 成功 | - |
| `10000-19999` | 通用错误 | 10001=参数校验失败, 10002=未登录, 10003=无权限 |
| `20000-29999` | 用户模块 | 20001=用户不存在, 20002=密码错误, 20003=账号已禁用 |
| `30000-39999` | 订单模块 | 30001=订单不存在, 30002=库存不足, 30003=订单已取消 |
| `40000-49999` | 支付模块 | 40001=余额不足, 40002=支付超时, 40003=退款失败 |
| `50000-59999` | 系统模块 | 50001=服务降级, 50002=限流, 50003=第三方调用失败 |

> 新模块接入时，必须先向架构组申请错误码段，**严禁自行占用**。

### 2.3 错误码定义文件规范

```typescript
// error-codes.ts — 必须集中管理，严禁在业务代码中硬编码魔法数字

export const ErrorCode = {
  // 通用
  PARAM_VALIDATION_FAILED: { code: 10001, message: "参数校验失败" },
  UNAUTHORIZED:           { code: 10002, message: "未登录或登录已过期" },
  FORBIDDEN:              { code: 10003, message: "无操作权限" },

  // 用户
  USER_NOT_FOUND:         { code: 20001, message: "用户不存在" },
  PASSWORD_INCORRECT:     { code: 20002, message: "密码错误" },
  USER_DISABLED:          { code: 20003, message: "账号已被禁用" },
} as const;
```

---

## 三、全局异常处理

### 3.1 强制要求：所有接口必须被全局异常处理器兜底

**严禁在业务代码中裸抛异常。** 所有可预见的业务错误必须通过抛出统一的 `BusinessException` 传递，由全局异常处理器统一捕获并转换为标准响应体。

### 3.2 业务异常类

```typescript
// business-exception.ts
export class BusinessException extends Error {
  constructor(
    public readonly code: number,
    public readonly message: string,
  ) {
    super(message);
    this.name = "BusinessException";
  }
}
```

### 3.3 全局异常处理器

```typescript
// global-exception-filter.ts
@Catch()
export class GlobalExceptionFilter implements ExceptionFilter {
  catch(exception: unknown, host: ArgumentsHost) {
    const response = host.switchToHttp().getResponse();

    if (exception instanceof BusinessException) {
      // 业务异常 → 标准响应体
      response.status(200).json({
        code: exception.code,
        message: exception.message,
        data: null,
      });
      return;
    }

    // 未知异常 → 500 + 通用提示，**绝对不能把堆栈泄露给前端**
    logger.error("未捕获异常", exception);
    response.status(200).json({
      code: 50000,
      message: "服务内部错误，请联系管理员",
      data: null,
    });
  }
}
```

### 3.4 绝对禁止

```typescript
// ❌ 裸抛 Error
throw new Error("用户不存在");

// ❌ 直接返回非标准格式
return res.status(400).json({ error: "参数错误" });

// ❌ try-catch 后吞掉异常
try { ... } catch(e) { /* 静默 */ }

// ❌ 把堆栈信息返回给前端
return { code: 50000, message: e.stack, data: null };
```

---

## 四、分页请求规范

### 4.1 入参格式

所有分页接口的请求参数必须遵循以下结构：

```typescript
interface PageRequest {
  page: number;     // 页码，从 1 开始，默认 1
  pageSize: number; // 每页条数，默认 20，最大 100
  sortBy?: string;  // 排序字段（可选）
  sortOrder?: "asc" | "desc"; // 排序方向（可选，默认 desc）
}
```

**强制约束：**
- `page` 最小值为 1，传入 0 或负数直接返回参数校验失败。
- `pageSize` 上限 100，超过 100 的请求一律截断为 100。**不允许一次性拉取全量数据**，要全量导出走专门的导出接口。
- `sortBy` 只允许白名单字段，禁止用户传入任意字段名（SQL 注入风险）。

### 4.2 出参格式

```typescript
interface PageResponse<T> {
  list: T[];       // 当前页数据
  total: number;   // 总记录数
  page: number;    // 当前页码
  pageSize: number; // 每页条数
}
```

### 4.3 完整分页响应示例

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "list": [
      { "id": 1, "name": "张三", "role": "admin" },
      { "id": 2, "name": "李四", "role": "user" }
    ],
    "total": 58,
    "page": 1,
    "pageSize": 20
  }
}
```

### 4.4 空页处理

当请求页码超过实际页数时：
- `list` 返回空数组 `[]`
- `total` 返回真实总记录数
- **禁止返回 404**，空页不是错误

---

## 五、接口命名规范

| 操作 | Method | 路径 | 说明 |
|------|--------|------|------|
| 查询列表 | GET | `/api/users` | 分页查询 |
| 查询详情 | GET | `/api/users/:id` | 单条查询 |
| 创建 | POST | `/api/users` | 新增资源 |
| 全量更新 | PUT | `/api/users/:id` | 整体替换 |
| 部分更新 | PATCH | `/api/users/:id` | 局部更新 |
| 删除 | DELETE | `/api/users/:id` | 删除资源 |

> 禁止用动词做路径：`/api/getUser`、`/api/deleteOrder` 一律打回。

---

## 六、本规范的执行

1. Code Review 时，规范检查项为 **Blocker 级别**，不通过不准合并。
2. 新项目上线前，架构组进行规范合规审查。
3. 本规范解释权归技术架构组所有，有任何异议请提 RFC，不要自行变通。
